<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
        integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
    <div class="container">
        <header>
            <div class="compass-icon"><img src="/img/compass.svg" alt=""></div>
        </header>
        <div class="weather-title">
            <h1>Simply Weather</h1>
            <p>View the current weather for your city</p>
        </div>

        <form action="/" method="POST">
            <input type="text" name="city" class="buttonn" style="text-align: center" placeholder="Enter a city">
            <button type="submit" style=" padding-inline: 20px;">Go</button>
        </form>

        <div class="result">
            <% if(temp!==null) {%>
                <h1 class="temp">
                    <%= temp %>
                </h1>
                <%}%>

                    <% if(icon!==null) {%>
                        <div class="icon-des">
                            <img class="icon" src="http://openweathermap.org/img/w/<%= icon %>.png" alt="">
                            <%}%>
                                <% if(des!==null) {%>
                                    <h3>
                                        <%= des %>
                                    </h3>
                                    <%}%>
                        </div>


                        <% if(city!==null) {%>
                            <p class="city">
                                <%=city%>
                            </p>
                            <%}%>
        </div>
    </div>

    <form action="/logout?_method=DELETE" class="buttonn" style="margin: 2rem 0; 
     text-align: center" method="POST">
        <button type="submit" style="text-align: center; padding-inline: 20px;">Log Out</button>
        <!-- <button type="submit" class='btn btn-success pull-right'>Log Out</button> -->
    </form>

</body>

</html>
